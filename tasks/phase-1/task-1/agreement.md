# Agreement — T001: Project Setup & Repository Init
## Status: APPROVED
## Approved: 2026-03-11 (auto-approved by atlas-run --full-permission)

## Scope
- Create the Unity project repository at `/Users/ofek/Projects/Claude/AtlasTest/CultRoguelite/cultrougelite/`
- Set up the full directory tree under `Assets/` (Scripts, ScriptableObjects, Prefabs, Scenes, Art, Audio, Animations, Editor, Settings with all subdirectories)
- Create `.gitignore` for Unity (Library/, Temp/, Logs/, obj/, Build/, UserSettings/, *.csproj, *.sln, etc.)
- Create `.gitattributes` with Unity merge strategies and LFS tracking rules
- Create `Packages/manifest.json` with com.unity.inputsystem, com.unity.2d.animation, com.unity.2d.tilemap, com.unity.2d.tilemap.extras
- Initialize git repository and create initial commit

## Not In Scope
- Unity Editor project settings (ProjectSettings/ contents generated by Unity)
- Any C# scripts or game code
- Art assets, audio files, or scene files
- CI/CD configuration

## Checkpoint Parameters
- All specified directories exist and are tracked (via .gitkeep or equivalent)
- `Packages/manifest.json` is valid JSON containing all four required package references
- `.gitignore` covers standard Unity exclusions (Library, Temp, Logs, obj, Build)
- `.gitattributes` is present with Unity-appropriate rules
- Git repository is initialized with a clean working tree after initial commit

## Skills Used
- Unity project structure conventions
- Git repository initialization

## Risks
- Package versions in manifest.json may need adjustment for the target Unity version
- .gitattributes LFS rules depend on whether Git LFS is installed on the machine
